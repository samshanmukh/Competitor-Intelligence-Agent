node_modules/
client/node_modules/
client/dist/
.env
*.sqlite
*.sqlite-journal
*.db
server/db/data/
.DS_Store
*.log

# Local env files (never commit — they bake dev values into prod builds)
.env*.local
client/.env*.local

# Next.js build output (regenerated on every build — never commit)
client/.next/
.next/

# MCP config contains secrets
.mcp.json
.vercel

import MovesClient from '../../components/MovesClient';

export const metadata = {
  title: 'Next moves',
  description: 'Turn market and competitor signals into a focused action brief.',
};

export default function Page() {
  return <MovesClient />;
}

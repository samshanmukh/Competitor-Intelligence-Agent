import UsageClient from '../../components/UsageClient';

export const metadata = {
  title: 'Usage',
  description: 'Review lab activity and manage retained workspace data.',
};

export default function Page() {
  return <UsageClient />;
}

import WinLossClient from '../../components/WinLossClient';

export const metadata = {
  title: 'Win / loss',
  description: 'Turn sales notes into objections, talk tracks, and battlecard updates.',
};

export default function Page() {
  return <WinLossClient />;
}

import AnalystClient from '../../components/AnalystClient';

export const metadata = {
  title: 'Ask Mira',
  description: 'Chat with Mira and Pricing analysts inside the app using your workspace data.',
};

export default function AnalystPage() {
  return <AnalystClient />;
}

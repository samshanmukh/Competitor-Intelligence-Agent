import ChangesClient from '../../components/ChangesClient';

export const metadata = {
  title: 'Changes',
  description: 'Review detected competitor changes and their potential impact.',
};

export default function ChangesPage() {
  return <ChangesClient />;
}

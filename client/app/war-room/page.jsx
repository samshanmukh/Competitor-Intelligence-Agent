import WarRoomClient from '../../components/WarRoomClient';

export const metadata = {
  title: 'War room',
  description: 'Track competitive deals, talk tracks, and risks in one place.',
};

export default function Page() {
  return <WarRoomClient />;
}
